# 项目协作约定

- 所有用户说明、注释与错误提示优先中文，文本文件和终端输出使用 UTF-8。
- 优先使用 PowerShell 7；Python 命令使用 `-X utf8`。
- 本项目是独立的本地画质助手。不得修改旁边的 Kazumi 源码、安装文件或规则配置，不把原项目作为运行时依赖。
- 读取用户已安装的 `plugins.json` 必须只读；验证记录、真实媒体地址、用户路径配置、模型和解释器放在被 Git 忽略的 `data/`、`.runtime/`、`.venv/` 中。
- 不提交本机网站规则、Cookie、令牌或模型权重。
- 画质评分必须来自实际模型推理。失败、缺失或不满足共同内容要求时不得伪造分数或排名。
- 当前模型尚未经过动画领域人工校准，不把模型分数表述为百分制正确率或绝对画质结论。
- 本地服务仅监听 `127.0.0.1`；保留同源、会话令牌及进程身份检查。
- 修改规则解析、排名、任务取消或启动逻辑时，先补能够复现问题的测试。运行 `python -X utf8 -m unittest discover -s tests -v` 和前端契约测试。
